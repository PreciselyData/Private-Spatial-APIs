# Example Environment Variables for Docker Compose
# Copy this file to .env and fill in with your actual values
# NEVER commit .env file to version control!

# Docker Registry Configuration
IMAGE_REGISTRY=your-registry.example.com
TAG=latest

# Data Path
SPATIAL_PATH=/path/to/your/spatial/data

# OAuth2 Configuration
# SECURITY WARNING: Generate a secure random secret for production!
# Use: openssl rand -hex 32
OAUTH2_CLIENT_SECRET=generate-a-secure-secret-here

# MongoDB Configuration (if needed)
MONGO_ROOT_USERNAME=your-mongo-username
MONGO_ROOT_PASSWORD=generate-a-secure-password-here

# Keycloak Configuration (if needed)
KEYCLOAK_ADMIN_USER=your-admin-username
KEYCLOAK_ADMIN_PASSWORD=generate-a-secure-password-here

