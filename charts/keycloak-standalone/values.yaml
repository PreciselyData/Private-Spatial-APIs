# Default values for platform.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Set these values using --set flags or a secure values file during deployment
# Example: helm install --set adminUser=<user> --set adminPassword=<secure-password>
adminUser: ""
adminPassword: ""
hostname: